// import { of, Observable } from 'rxjs';
// import { AccountItem } from '../../libs/api';

// export class AccountServiceMock {
//   listOfAccounts$ = of([]);
//   getListOfAccounts = jest.fn();
//   setAccountClosing = jest.fn().mockReturnValue({ subscribe: () => {} });
//   cloneAccount = jest.fn().mockReturnValue({ subscribe: () => {} });
//   getAccount = jest.fn();

// }
