{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": ["node"]
  },
  "exclude": [
    "test.ts",
    "testing",
    "**/*.mock.ts",
    "**/*.spec.ts",
    "**/*.stories.ts",
    "**/*.story.ts"
  ]
}
